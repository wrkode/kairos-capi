apiVersion: bootstrap.cluster.x-k8s.io/v1beta2
kind: KairosConfig
metadata:
  name: invalid-worker-no-token
  namespace: default
spec:
  role: worker
  distribution: k0s
  # NOTE: no workerToken and no workerTokenSecretRef → should be rejected
